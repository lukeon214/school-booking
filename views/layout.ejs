<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Forms</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="background-pattern"></div>

  <div class="layout">
    <% if (typeof noSidebar === 'undefined' || !noSidebar) { %>
      <aside class="sidebar">
        <h2>📋 Booking</h2>
    
        <% if (user) { %>
          <div class="nav-section">
            <a href="/dashboard"><button class="nav-btn">Dashboard</button></a>
            <a href="/create"><button class="nav-btn">Create Form</button></a>
          </div>
    
          <div class="nav-bottom">
            <a href="/logout"><button class="nav-btn danger">Logout</button></a>
          </div>
        <% } %>
      </aside>
    <% } %>

    <main class="main">
      <%- body %>
    </main>
  </div>
</body>
</html>